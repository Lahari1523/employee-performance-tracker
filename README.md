# Employee Performance Tracker

This project tracks employee performance using Python and SQLite.

## Features
- Add, update, and view employee records
- Generate reports to find top performers
- Simple Python and SQLite based

## How to Run
1. Run `database_setup.py` to create the database.
2. Run `main.py` to add/view employee data.
